const { json } = require("../db/db")

exports.setCredentials = (mail, pass, code) => {

}

exports.checkCode = (code, mac) => {
    // let db = json;
    // db.codes.forEach(dbCode => {
    //     if (code !== dbCode.code && mac !== dbCode.mac) {
    //         console.log()
    //     }
    // });
}