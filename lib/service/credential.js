const { Credentials } = require("../model/db")

exports.setCredentials = (mail, pass, code) => {
  // let found = await Credentials.findOne({code})
  // if (found.code){
  //     let {mail, pass, code} = found
  // }
  // else {

  // }
}

exports.checkCode = (code, mac) => {

}